import * as taskControllers from './tasks';
import * as journalControllers from './journal';

const controllers = {
  tasks: taskControllers,
  journal: journalControllers
};

export default controllers;
